# SDCodex environment — copy to .env and fill in.
SDCODEX_PORT=5000
SECRET_KEY=
DB=./db
MODELS=./models
PUID=1000
PGID=1000
UMASK=002

# GitHub token for private plugin repos (Settings -> Plugins can also set this)
GITHUB_TOKEN=

# Plugin volumes (managed by Settings -> Plugins, appended below)
